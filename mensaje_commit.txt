refactor: elimina tabla "Progreso por trabajador" del dashboard admin

src/app/admin/dashboard/page.tsx

ELIMINADO — exclusivo de la tabla eliminada:
· interface WorkerProgress (id, full_name, sede, area_trabajo,
  courses_completed, status, last_activity)
· Query coursesForStatus (courses → id, deadline, target_areas,
  is_published=true) — solo alimentaba el map de workerProgress
· Variable today (new Date con setHours) — solo usada en hasOverdue
  dentro del map
· workerProgress: WorkerProgress[] completo — map sobre workersData
  que calculaba completedCourseIds, relevantCourses, hasOverdue,
  status, lastActivity por cada trabajador
· Bloque JSX de la tabla (header "Progreso por trabajador" + botón
  "Ver todos" + tabla con columnas Trabajador / Sede / Área /
  Completados / Estado / Última actividad)
· Función StatusBadge (badge al_dia / en_progreso / atrasado)

CONSERVADO — usado en otras secciones del dashboard:
· allProgress (course_progress) → recentActivity (actividad reciente)
  y workersWithNoProgress (hero banner)
· allCertificates (certificates) → totalCertificates (card métricas)
· workersData (profiles) → sede1Workers/sede2Workers (comparativa
  por sede) y recentActivity
· Resto del dashboard sin cambios: hero banner, cards de métricas,
  comparativa por sede, actividad reciente, cursos más completados,
  certificados este mes
