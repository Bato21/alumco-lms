feat: CRUD completo de trabajadores + áreas múltiples por perfil (Fase 10)

## Base de datos
- ALTER TABLE profiles ALTER COLUMN area_trabajo TYPE area_trabajo_tipo[]
  USING ARRAY[area_trabajo]::area_trabajo_tipo[]
- Migración de valor único a array manteniendo datos existentes
- ALTER COLUMN area_trabajo SET DEFAULT '{}'
- DROP y recreación de reporte_avance para soportar array:
  usa area_trabajo[1] como valor representativo en la vista

## Server Actions — src/lib/actions/trabajadores.ts
- updateWorkerAction: edita full_name, rut, sede y area_trabajo[]
  con validación Zod, createAdminClient(), revalidatePath doble
- suspendWorkerAction: cambia status a 'suspendido'
- reactivateWorkerAction: cambia status a 'activo'
- getWorkerDetailAction: retorna perfil + progreso de cursos
  + certificados para la vista de detalle

## Componentes creados
- src/components/alumco/WorkerEditPanel.tsx — panel lateral para
  editar datos del trabajador con dropdown multi-select de áreas,
  campos de nombre/RUT/sede y botón de suspensión con confirmación
- src/app/admin/trabajadores/WorkersTable.tsx — tabla de activos
  con filtros por nombre/sede/área, badges de áreas múltiples
  (máx 2 visibles + contador), links a detalle y botón editar
- src/app/admin/trabajadores/SuspendedTable.tsx — tabla de
  suspendidos con botón reactivar y badge de estado rojo
- src/app/admin/trabajadores/[id]/page.tsx — vista detalle con
  card de perfil, 4 stats, tabla de progreso por curso ordenada
  y sección de certificados con links
- src/app/admin/trabajadores/[id]/WorkerActions.tsx — Client
  Component con botones editar/suspender/reactivar

## Página trabajadores actualizada
- Nueva tab "Suspendidos" con contador
  Orden de tabs: Activos | Suspendidos | Solicitudes pendientes
- Tab activos reemplazada por WorkersTable con filtros reactivos
- Queries adicionales para trabajadores suspendidos

## ApprovalPanel actualizado
- Selector de área con dos modos: Una área / Varias áreas
- Modo simple: select dropdown igual al anterior
- Modo múltiple: dropdown con checkboxes visuales, contador
  de selección y botón Listo
- Validación: mínimo 1 área seleccionada antes de aprobar
- formData.getAll('area_trabajo') para enviar múltiples valores

## Tipos TypeScript actualizados
- AreaTrabajo[] en profiles (era AreaTrabajo)
- UpdateWorkerSchema con z.array(...).min(1)
- ApproveSchema con z.array(...).min(1)
- approveWorkerAction lee formData.getAll('area_trabajo')